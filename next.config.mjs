/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Optional: 'standalone' creates a self-contained bundle with minimal node_modules for server hosting / Docker
  // output: 'standalone',
};

export default nextConfig;

